import { CircuitConfig } from '@unirep/circuits';
export type SchemaField = {
    name: string;
    type: string;
    updateBy: 'sum' | 'replace';
};
export type Attestation = {
    fieldIndex: number;
    change: bigint;
};
export declare class DataSchema {
    schema: any[];
    config: CircuitConfig;
    constructor(schema: any, config?: CircuitConfig);
    parseSchema(schema: SchemaField[]): any[];
    buildAttestation(change: {
        name: string;
        val: bigint;
    }): Attestation;
    buildAttestations(changes: {
        name: string;
        val: bigint;
    }[]): Attestation[];
    parseData(data: bigint[]): any;
}
